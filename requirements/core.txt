# Galactic AI — CORE (always installed, ~120 MB)
# Chat, Control Deck, all cloud/Ollama providers, scheduler, CLI.
# Everything else is an optional feature group — see install.py.

# ─── Web framework & networking ──────────────────────────────────────────────
aiohttp>=3.10.11
httpx[http2]>=0.27.0
pyyaml>=6.0.1
jinja2>=3.1.0
requests>=2.32.3
urllib3>=2.0.0

# ─── AI provider SDKs (cover 14+ providers) ──────────────────────────────────
openai>=1.40.0               # OpenAI + all OpenAI-compatible (Groq, Cerebras, xAI, …)
anthropic>=0.34.0            # Anthropic Claude
google-genai>=1.0.0          # Google Gemini + Imagen
google-auth>=2.30.0          # Google auth / OAuth

# ─── Web fetch & parsing ─────────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ─── Scheduling ──────────────────────────────────────────────────────────────
apscheduler>=3.10.0

# ─── Security / remote access ────────────────────────────────────────────────
cryptography>=44.0.0         # TLS certs for remote access
qrcode[pil]>=8.0             # QR pairing for mobile

# ─── Images (basic) ──────────────────────────────────────────────────────────
Pillow>=10.4.0

# ─── Utilities & terminal UI ─────────────────────────────────────────────────
psutil>=5.9.0
python-dotenv>=1.0.0
rich>=13.7.0
prompt_toolkit>=3.0.0
tqdm>=4.66.0
