# Galactic AI — FULL dependency set (every feature)
#
# ┌──────────────────────────────────────────────────────────────────────┐
# │  RECOMMENDED: use the installer instead of pip.                      │
# │                                                                      │
# │      python install.py                                               │
# │                                                                      │
# │  It lets you pick features (Lite / Full / Custom), skips the ~2 GB   │
# │  ML stack if you don't want it, picks CPU vs CUDA torch for you,     │
# │  and verifies the result.                                            │
# └──────────────────────────────────────────────────────────────────────┘
#
# This file installs EVERYTHING (~4 GB) and is kept for compatibility and
# for CI. Individual feature groups live in requirements/*.txt.

-r requirements/core.txt
-r requirements/memory.txt
-r requirements/voice-tts.txt
-r requirements/voice-stt.txt
-r requirements/browser.txt
-r requirements/desktop.txt
-r requirements/ocr.txt
-r requirements/documents.txt
-r requirements/bridges.txt
